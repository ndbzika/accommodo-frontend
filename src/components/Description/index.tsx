'use client'

import { Typography } from '@mui/material'

import styles from './styles.module.scss'

export const Description = () => {
  return (
    <Typography className={styles.description}>
      Descubra nossos serviços e entre para a equipe!
    </Typography>
  )
}